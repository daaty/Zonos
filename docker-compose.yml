version: '3.8'

services:
  zonos:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "7860:7860"
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - GRADIO_SHARE=False
      - GRADIO_SERVER_PORT=7860
    deploy:
      replicas: 1